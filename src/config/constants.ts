// Дизайн-разрешение (портрет). Phaser Scale.FIT масштабирует под экран устройства.
export const DESIGN_WIDTH = 720;
export const DESIGN_HEIGHT = 1280;

// Палитра-заглушка для примитивов MVP (визуал придёт позже).
export const COLORS = {
  bg: 0x0e0f12,
  city: 0x1a2f1a,
  fence: 0x444a55,
  base: 0x15171c,
  accent: 0x9fe870,
  text: 0xcccccc,
} as const;

/**
 * Эталонный размер «фрейма» иконки оружия в пикселях PNG-экспорта из Figma.
 *
 * В Figma все 19 оружий нарисованы в COMPONENT'ах 136×136 (set "Weapon" 164:69); каждый
 * дизайнер позиционировал/масштабировал ВРУЧНУЮ в своём фрейме — поэтому винтовка
 * длиннее ножа, а топор компактнее автомата. При экспорте `pngScale: 2` фрейм 136
 * становится 272 пикселя; некоторые оружия торчат за пределы фрейма (Figma не клипает
 * компоненты), поэтому PNG имеют разный bbox (272-331 px). Чтобы СОХРАНИТЬ
 * дизайнерские относительные размеры — все весы масштабируем по этому эталону,
 * а НЕ по `max(iw, ih)` каждой картинки (иначе длинная винтовка визуально стала бы
 * того же размера, что и короткий нож).
 */
export const WEAPON_FRAME_PX = 272;

/**
 * Доп. масштабный коэффициент для иконок ЛУТБОКСОВ (cheap/medium/elite) — применяется
 * поверх их «target size» в каждом из 3 мест отрисовки: merge-плитка, inventory, chest
 * reward. Делает PNG-арт лутбокса компактнее на (1 - LOOTBOX_ICON_SCALE) × 100%
 * относительно его контейнера. 0.7 ≈ -30% от исходного «full-fit» размера.
 */
export const LOOTBOX_ICON_SCALE = 0.7;

/**
 * Минимальный тир оружия, для которого на мердж-плитке показывается красный «NEW!»-ярлык
 * (слева-сверху), пока игрок не сходил с ним в бой. Для T1..T(MIN-1) ярлык не рисуем —
 * считаем эти тиры «обычными» (игрок видит их на старте каждого прогона как stack-output
 * мастерской и инвентаря). 5 — первый «не-стартовый» тир, появление которого хочется
 * подсветить. См. SaveState.battledTiers + WorldScene.goBattle + MergeBoard.makeTile.
 */
export const NEW_BADGE_MIN_TIER = 5;

// Цвет плитки оружия по тиру (1-based; index 0 — заглушка). Палитра — из Figma макета
// weapon_frame (164:341): цвет берётся с плашки tier-индекса каждого тира. Используется:
//   • как fallback в `MergeBoard.makeTile` если PNG-рамка не загрузилась;
//   • для ring-strokeStyle вокруг бойца в бою (`FightersController.renderFighterWeapon`);
//   • для tint placeholder-иконки на кнопке Produce (`MainScreen.refresh`).
// Палитра повторяется циклом из 8 базовых цветов (figma-дизайнер так задумал, "категории
// оружия" по 8 тиров): T1 — коричневый (дерево), T2/9/17 — зелёный, T3/11/19 — синий,
// T4/12 — красный, T5/13 — фиолет, T6/14 — оранж, T7/15 — индиго, T8/16 — розовый,
// T10/18 — золотой (с лёгкой разницей оттенков между ними).
export const TIER_COLORS: number[] = [
  0x333333,                                                          // 0: заглушка
  0x925326, 0x18a31d, 0x1a97d5, 0xe25656, 0xa94bd4, 0xee862a, 0x7268ff, 0xef82cb, // T1-T8
  0x18a31d, 0xe5c233, 0x1a97d5, 0xe25656, 0xa94bd4, 0xee862a, 0x7268ff, 0xef82cb, // T9-T16
  0x18a31d, 0xe5c133, 0x1a97d5,                                                   // T17-T19
];

export const UI = {
  hudBg: 0x0a0b0e,
  slot: 0x20242c,
  slotStroke: 0x3a414d,
  btn: 0x2e7d32,
  btnDisabled: 0x3a3f47,
  btnText: '#ffffff',
} as const;
