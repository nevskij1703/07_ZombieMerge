// Дизайн-разрешение (портрет). Phaser Scale.FIT масштабирует под экран устройства.
export const DESIGN_WIDTH = 720;
export const DESIGN_HEIGHT = 1280;

// Палитра-заглушка для примитивов MVP (визуал придёт позже).
export const COLORS = {
  bg: 0x0e0f12,
  city: 0x1a2f1a,
  fence: 0x444a55,
  base: 0x15171c,
  accent: 0x9fe870,
  text: 0xcccccc,
} as const;

/**
 * Эталонный размер «фрейма» иконки оружия в пикселях PNG-экспорта из Figma.
 *
 * В Figma все 19 оружий нарисованы в COMPONENT'ах 136×136 (set "Weapon" 164:69); каждый
 * дизайнер позиционировал/масштабировал ВРУЧНУЮ в своём фрейме — поэтому винтовка
 * длиннее ножа, а топор компактнее автомата. При экспорте `pngScale: 2` фрейм 136
 * становится 272 пикселя; некоторые оружия торчат за пределы фрейма (Figma не клипает
 * компоненты), поэтому PNG имеют разный bbox (272-331 px). Чтобы СОХРАНИТЬ
 * дизайнерские относительные размеры — все весы масштабируем по этому эталону,
 * а НЕ по `max(iw, ih)` каждой картинки (иначе длинная винтовка визуально стала бы
 * того же размера, что и короткий нож).
 */
export const WEAPON_FRAME_PX = 272;

// Цвет плитки оружия по тиру (1-based; index 0 — заглушка). Fallback при отсутствии иконки;
// также используется как «акцент» (рамка плитки, цвет tier-индикатора и т.п.).
// 19 тиров — палитра проходит через бэнды: дерево/металл (T1-4) → сталь (T5-7) →
// чёрный полимер (T8-11) → армейский (T12-15) → яркий неон/sci-fi (T16-19).
export const TIER_COLORS: number[] = [
  0x333333,                                   // 0: заглушка
  0x8a6a3a, 0x8a8f98, 0xa9b1ba, 0xc98a3a,    // T1-T4: дерево/металл/ржавчина
  0xd9d2c0, 0xb03a3a, 0xff6a00,              // T5-T7: сталь / тактик / бензопила-оранжевый
  0xff3a3a, 0x3a7bd5, 0x3ad5a0,              // T8-T10: огнемёт-красный / пистолет-синий / зелёный
  0xd5c43a, 0xd53a9b, 0x7a3ad5,              // T11-T13: жёлтый / магента / фиолет
  0x00e5ff, 0x9affff,                         // T14-T15: бирюза / голубой (рейлган)
  0xffaa00, 0xff5500, 0x00ffaa, 0xff00ff,    // T16-T19: оранжевый / алый / неон-зелёный / магента (лазертаг)
];

export const UI = {
  hudBg: 0x0a0b0e,
  slot: 0x20242c,
  slotStroke: 0x3a414d,
  btn: 0x2e7d32,
  btnDisabled: 0x3a3f47,
  btnText: '#ffffff',
} as const;
